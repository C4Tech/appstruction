<fieldset class="form-group">
    {{#each fields}}
        {{#if show}}
            {{#if fieldTypeSelect}}
                <select id="{{../../../routeType}}-{{name}}-{{../../../cid}}"
                        name="{{name}}"
                        class="field {{fieldType}} type form-control"
                        data-placeholder="{{placeholder}}"
                >
                    <option></option>
                    {{#each options}}
                        <option value="{{id}}">{{text}}</option>
                    {{/each}}
                </select>
                {{#unless append}}
                    <br />
                {{/unless}}
            {{else}}
                {{#if inputGroup}}
                    <div class="input-group">
                    {{#if inputGroupPrepend}}
                        <span class="input-group-addon">{{inputGroupValue}}</span>
                    {{/if}}
                {{/if}}
                <input type="{{fieldType}}"
                       id="{{../../../routeType}}-{{name}}-{{../../../cid}}"
                       name="{{name}}"
                       placeholder="{{placeholder}}"
                       value="{{value}}"
                       class="field {{../../../routeType}} {{name}} form-control"
                />
                {{#if inputGroup}}
                    {{#if inputGroupAppend}}
                        <span class="input-group-addon">{{inputGroupValue}}</span>
                    {{/if}}
                    </div>
                {{/if}}
            {{/if}}
            {{#if append}}
                {{{append}}}
            {{/if}}
        {{/if}}
    {{/each}}
</fieldset>
