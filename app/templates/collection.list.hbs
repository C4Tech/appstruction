{{#if title}}
    <div class="peanl-heading">
        <h4 class="panel-title">{{title}}</h4>
    </div>
{{/if}}
<div class="panel-body">
    <div class="{{type}} text-success">
        Cost: $<span class="{{type}} cost">{{cost}}</span>
    </div>
    <ul class="{{type}} items list-group"></ul>
</div>
